export interface User {
  name: string;
  email: string;
}

export interface UserName {
  name: string;
}

export interface UserEmail {
  email: string;
}

export interface ValidCheck {
  value: string;
}
